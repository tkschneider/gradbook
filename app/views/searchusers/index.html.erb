<% content_for :search do %>
  <%= search_form_for @search do %>
      <div class="row">
        <div class="large-8 small-8 columns">
          <%= f.search_field :first_name_or_last_name_or_email_addr_cont %>
        </div>
        <div class="large-4 small-4 columns">
          <%= f.submit "search", :class => "button" %>
        </div>
      </div>
  <% end %>
<% end %>
  
<%= render 'results' %>
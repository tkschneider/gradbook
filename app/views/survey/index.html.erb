<div class="row">
  <div class="large-12 columns">
    <table>
      <tr>

        <th>Survey</th>

        <th colspan="4"></th>
      </tr>
  <% @survey.each do |s| %>
    <tr>

      <td><%= s.survey_name%></td>

      <td><%= link_to 'take', action: 'take', :id => s, :survey_id => s %></td>
          </tr>
      <% end %>
    </table><br>
          </tr>

    </table><br>
    <%= link_to 'Create', {controller: "survey_admin", action: 'new'} %>
  </div>
</div>
